// Domain event dispatching is implemented in Bookify.Infrastructure.Persistence.UnitOfWork
// to maintain Clean Architecture (Application layer has no EF Core dependency).
// See UnitOfWork.SaveChangesAsync for domain event dispatch logic.
